# Tempr — Progress

> **Single source of truth for where the build stands.** Update after every
> completed task per the living-docs rule in CLAUDE.md. Phase definitions
> live in docs/16-roadmap.md; business scope in docs/PRODUCT.md.

## Current status

- **Last completed**: **Fix: mouse wheel scrolls the result grid vertically again** (branch `fix/todo-scroll-direction`, PR #17, 2026-09-11) — `ResultGrid`'s horizontal column container and its `uniform_list` rows both set `restrict_scroll_to_axis` (→ D29, 11-gpui.md hard rule 6) and the column area is `min_w_full`; reproduced and re-verified headlessly on Xvfb with an XTest injector (session log). Still open: the user's hands-on Wayland check, grid and editor, with and without shift (TODO Now). Before that: **Phase 3 stage 2 (catalog persistence)** — `SchemaObjectId::derived` replaces the random UUID with a UUIDv5 over the connection id and `(kind discriminant, native_id)` (→ D25); `SchemaSnapshot` gained `keywords`/`fingerprints` (`#[serde(default)]`); a new `.tcat` on-disk format (magic + version + flags + FNV-1a content hash header, `postcard` body over a private externally-tagged `CatalogSnapshot`/`CatalogObject` mirror, since `SchemaObject`'s internally-tagged JSON shape can't decode through postcard's non-self-describing deserializer) lives behind `Storage::catalog_cache`/`CatalogCacheFile` (→ D27, corrected this stage: the Decision sentence named `SchemaSnapshot` where the wire type is actually the private mirror; body codec switched from `bincode` to `postcard` in a same-branch follow-up → D28); `SchemaService::with_cache` persists on `refresh` (best-effort, skips the rewrite when `snapshot_content_hash` is unchanged) and loads via `load_cached` without touching the database; `refresh`/`refresh_incremental` now scope introspection to `SchemaScope::SearchPath`, pass through `estimated_rows`/view definitions, and drop children whose parent fell out of scope; `refresh_incremental` diffs the driver's `(oid, xmin)` fingerprint sweep against the cached fingerprints and re-introspects only touched schemas, splicing the result in, with fallback to a full refresh in four cases (no cached fingerprints, no driver support, an unseen object, >40% touched) (→ D26); it now returns a `RefreshPath` (`Unchanged` / `Incremental{schemas, touched}` / `FullRefresh(FullRefreshReason)`) so a caller — and a test — can tell which path actually ran, not just infer it from the data. Review of the first live-server test found it exercised the full-refresh fallback, not the splice (`ADD COLUMN` mints a fingerprint key the cache has never seen, so `delta.added` is non-empty every time); it also surfaced a real bug the fallback had been masking — a delta with only dropped keys (no `changed`) always fell back to `FullRefresh(UnresolvedSchema)`, because the schema-resolution step only looked at `delta.changed` and treated finding zero schemas as failure, when a pure-drop delta legitimately needs none (`splice_objects` removes a dropped id by id, independent of which schemas were re-read). Fixed, and reproved against both live PostgreSQL containers with four ignored integration tests: ids survive a second refresh (with non-empty keywords/fingerprints); an `ALTER COLUMN ... TYPE` (changed-only delta) and a `DROP TABLE` (dropped-only delta) both take `RefreshPath::Incremental`, retype/remove the right object, and leave an untouched table's id and columns alone; an `ADD COLUMN` (an unseen key) takes `RefreshPath::FullRefresh(UnknownObject)` and still sees the new column; and a brand-new `SchemaService`/`ConnectionService` pair with no route to the database serves a full snapshot from the on-disk cache alone. All four share a `SCHEMA_REFRESH_LIVE_TEST_LOCK` (`SchemaScope::SearchPath` always includes `public`, so any test comparing a sweep across two points in time must not interleave with one that mutates `public`) — the other 9 tests that create/alter/drop a persistent `public` object take the same lock; the remaining tests run in parallel as before. (2026-09-08, branch `feat/ph3-catalog-persistence`)
- **Verified** (fix, 2026-09-11): `cargo fmt --all --check` ✅ · `cargo clippy --workspace --all-targets -- -D warnings` ✅ · `cargo test -p tempr_ui` ✅ 19 unit tests · Xvfb wheel injection: narrow and wide results, wheel/shift-wheel/tilt, pointer over and beside the rows, rows move on the vertical axis only and the container on the horizontal axis only. (Stage 2, 2026-09-08): `cargo fmt --all --check` ✅ · `cargo clippy --workspace --all-targets -- -D warnings` ✅ · `cargo test --workspace` ✅ 188 unit tests (0 failed) · `cargo test --workspace -- --ignored` ✅ 28 PostgreSQL integration tests (plain :55432 + TLS :55433, `DATABASE_URL_TLS` set) — 24 pre-existing + 4 this stage (`pg_object_ids_survive_a_second_refresh`, `pg_incremental_refresh_splices_a_changed_column_and_a_dropped_table`, `pg_incremental_refresh_falls_back_to_full_refresh_on_an_added_column`, `pg_catalog_is_readable_without_the_server`), stable across 10 repeated runs after the serialization fix above · `cargo deny check` ✅ advisories/bans/licenses/sources ok · one expected non-regression: `tempr_editor::buffer::tests::perf_large_batch_edit_is_linear` fails in this debug run (1.2 s vs its budget) — a release-only timing probe, unrelated to this stage (documented since D21/D22)
- **Next action**: merge PR #17 once the user confirms the wheel on Wayland; then Phase 3 stage 3 — the `tempr_intel` crate: an in-memory `CatalogCache` with lookups and a load probe, built on the `SchemaSnapshot` this stage now caches and identifies stably (see docs/superpowers/specs/2026-09-07-phase3-sql-intelligence-design.md §9). Completion is stage 5, after semantic analysis (stage 4). Both Phase 3 catalog-cache checklist boxes are now met; the remaining Phase 3 boxes (completion, semantic analyzer, diagnostics, hover, offline operation) are unstarted.

## Phase checklist

*Reconstructed from git history on 2026-07-13. Two commits total: initial project scaffold + full documentation suite. No implementation code yet.*

### Phase 0 — Foundations ✅ complete (2026-07-13)
- [x] Cargo workspace builds with zero warnings on Linux, macOS, Windows *(cargo clippy clean, 2026-07-13)*
- [x] Domain types (`Workspace`, `Connection`, `Query`, `Result`) compile and pass unit tests *(15 tests, 2026-07-13)*
- [x] Event bus dispatches typed events to registered handlers; ordering and delivery verified by tests *(6 tests, 2026-07-13)*
- [x] Service registry supports registration, lookup, and lifecycle (start/stop) with mock services *(5 tests, 2026-07-13)*
- [x] Workspace file format is read/write round-trip safe; malformed files produce structured errors *(4 manifest tests, 2026-07-13)*
- [x] Storage layer writes and reads structured data to platform-specific data directory *(6 storage tests, 2026-07-13)*
- [x] CI pipeline runs `cargo fmt --check`, `cargo clippy`, `cargo test`, `cargo deny` on every push *(.github/workflows/ci.yml, 2026-07-13)*
- [x] All 16 architecture docs exist and cross-reference correctly *(verified 2026-07-13)*

### Phase 1 — Connect & Run
- [x] `tempr_db` crate: `DatabaseDriver`, `DriverConnection` traits, `QueryStream`, `EngineId`, `SchemaScope`, `SchemaSnapshotEntry`, `DriverError` *(7 tests, 2026-07-14)*
- [x] `tempr_db_postgres` crate: `PostgresDriver`, `PostgresConnection`, `PostgresStream` (pinned `Pin<Box<RowStream>>`), `decode_value` (all PG types: bool, int, float, numeric, text, uuid, json, timestamp, date, time) *(6 tests, 2026-07-14)*
- [x] Extended `Value` with Uuid, Json, Timestamp, Date, Time, Numeric, Array, Custom; added `ValueType`, `ColumnSpec`, `Batch` to tempr_domain *(16 tests, 2026-07-14)*
- [x] `ConnectionService`: pool management, state tracking, `with_connection_fn` exclusive-access pattern, event publishing, proper Failed state on missing driver *(8 tests, 2026-07-14)*
- [x] `QueryService`: execute → stream → finalize lifecycle, `completed_runs` storage, event publishing *(4 tests, 2026-07-14)*
- [x] `SchemaService`: two-pass schema introspection (tables → columns/indexes via parent ID map), snapshot versioning, event publishing *(3 tests, 2026-07-14)*
- [x] Binary wires all services + PostgreSQL driver *(compiled, 2026-07-14)*
- [x] `ServiceError` extended with `QueryFailed`, `ConnectionNotFound`, `NotConnected` *(5 registry tests, 2026-07-14)*
- [x] 9 integration tests written (ignored, require DATABASE_URL): connect, insert/select, decode mixed types, insert returning, streaming, auth failure, schema refresh, syntax error, event publishing *(2026-07-14)*
- [x] SQL injection fixed in `snapshot_schema` — parameterized queries (`$1`, `$2`) *(2026-07-14)*
- [x] Decode layer handles PostgreSQL text-format booleans (`t`/`f`/`true`/`false`) and timestamps with timezone variants *(2026-07-14)*
- [x] GPUI dependency lands: `rust-toolchain.toml` 1.97.1, `gpui`+`gpui_platform`+`gpui_tokio` at one rev with `cargo deny` license gate, `tempr_ui` crate + `gpui_compat` shim, binary opens a 1200×800 window and starts services on the tokio bridge *(verified via `xwininfo`, 2026-09-03, → D17)*
- [x] PostgreSQL driver connects over TLS using Phase 0 connection config *(rustls, `TlsMode` on `Connection`; 5 TLS integration tests against a self-signed `ssl=on` PostgreSQL 16 — require/prefer encrypt, disable plaintext, verify-full rejects unknown CA; 2026-09-03, → D20)*
- [x] GPUI window renders with text input and scrollable result grid *(`Input` + `ResultGrid` + status bar; builds, opens against Docker PG; 2026-09-03; rendering confirmed by user screenshot: input, typed column headers, rows, status bar "Done — 100000 rows")*
- [x] Connection/auth/syntax errors produce user-visible messages *(status bar + grid pane: connection failures arrive as `ConnectionStateChanged{Failed}` through the `UiEvent` bridge — verified with wrong password; query errors arrive through the `execute_streaming` task result (the `QueryFinished{Error}` bus event is published too but the view does not consume it); syntax-error path covered by `pg_query_syntax_error`; 2026-09-03)*
- [x] Result grid displays streaming rows; smooth scroll for up to 100,000 rows *(streaming: `RowSink` → channel → `uniform_list`, integration test + user screenshot; scroll: `DebugScrollBenchmark` release, display on, user-run — 57.2 fps, avg 17.5 ms, p95 23.1 ms, max 59 ms, 61/603 frames > 20 ms under a worst-case 166-row jump per frame; 10% slow frames tracked in TODO; 2026-09-03)*

### Phase 2 — Editor ✅ complete (2026-09-07)
- [x] Rope buffer handles 10 MB documents with sub-millisecond insert/delete *(`tempr_editor::Buffer`; release probe on a 10 MB buffer: avg 1.73 µs, worst 12.1 µs per insert+delete; 9 unit tests; 2026-09-03, → D21)*
- [x] Tree-sitter PostgreSQL grammar produces incremental syntax tree *(`tree-sitter-sequel` 0.3 / `tree-sitter` 0.25; `Buffer` feeds `InputEdit`s and re-parses lazily; incremental == full parse in tests; release: 1.6 ms reparse on 10 MB realistic SQL, 150 ms on a 180k-statement dump; 2026-09-03, → D22)*
- [x] Statement detector identifies boundaries ($$, comments, string literals) *(derived from the tree-sitter tree: `SyntaxTree::statement_ranges`/`statement_at`; test covers `;` inside string literals, line and block comments, and `$tag$…$tag$` bodies; 2026-09-03, → D22)*
- [x] Command palette: opens via keybinding, fuzzy search, executes commands *(`Palette` over `CommandService::search`, dispatch via the typed catalog; search/ranking/dispatch unit-tested; **interactively verified 2026-09-07**: ctrl-shift-p opens, typing filters (37 searchable commands), enter dispatches against the editor, esc closes and restores focus, arrow keys scroll the list; two UI fixes landed during the check — full-width rows and `track_scroll` on the list)*
- [x] Keybindings configurable via workspace format; default map provided *(three layers applied at startup: catalog defaults ← `~/.config/tempr/settings.toml` ← `<workspace>/workspace.toml` `[keybindings]`, the last resolved from `TEMPR_WORKSPACE` or the current directory; `[]` unbinds, invalid keystrokes fall back to defaults, a corrupt file is non-fatal with a status-bar notice; verified end-to-end with `TEMPR_LIST_COMMANDS=1` — `RunQuery` rebound to `f5`, `DebugScrollBenchmark` unbound; 2026-09-05/2026-09-07, → D23, D24)*
- [x] Full editing ops on rope buffer (cursor, selection, copy/paste, undo/redo, line ops) *(`tempr_editor::{Selection, motion, edit_ops}`: grapheme/word/line/vertical motions with goal column; multi-cursor `insert_at`, `backspace`, `delete_forward`, `selected_text`, `delete_lines`, `duplicate_lines`, `move_lines`; each op is one atomic `Buffer::edit` batch; `undo_with_selections`/`redo_with_selections` restore cursors; 11 tests; UI wiring arrives with `EditorView`; 2026-09-05)*
- [x] "Execute statement under cursor" works end-to-end *(`EditorView` ctrl-enter → `Buffer::statement_at` (selection wins; `Error` fragments refused) → `EditorEvent::Run` → `MainWindow::run_sql` → `QueryService::execute_streaming` → grid; **interactively verified 2026-09-07** against Docker PG: `select 1 as a; select 2 as b;` on one line — cursor in the first statement returns column `a`, cursor in the second returns column `b`, ctrl-z undoes the typing, empty buffer reports "No statement under cursor."; 2026-09-05)*
- [x] Keyboard-only audit complete: every action listed with its keybinding *(`tempr_ui::commands::core_commands()` is the single list — 54 commands (33 editor), test `every_command_has_a_default_keystroke`, table in 11-gpui.md "Command Catalog", `TEMPR_LIST_COMMANDS=1`; 2026-09-05)*

### Phase 3 — Intelligence
- [x] Catalog cache loads full schema metadata from PostgreSQL and caches locally *(`.tcat` on `Storage::catalog_cache`; `SchemaService::with_cache`/`refresh`/`load_cached`; offline round-trip verified by `pg_catalog_is_readable_without_the_server`, 2026-09-08, → D25, D27)*
- [x] Cache refreshes incrementally; full refresh available on demand *(`refresh_incremental` diffs an `(oid, xmin)` sweep against cached fingerprints, re-introspects touched schemas, falls back to `refresh` past four thresholds, and reports which via `RefreshPath`; a changed column and a dropped table both take the incremental splice (`pg_incremental_refresh_splices_a_changed_column_and_a_dropped_table`), an added column deliberately falls back to a full refresh (`pg_incremental_refresh_falls_back_to_full_refresh_on_an_added_column`), 2026-09-08, → D26)*
- [ ] Completion provider: context-aware suggestions ranked by relevance
- [ ] Completion latency < 5 ms for 10,000 cached schema objects
- [ ] Semantic analyzer resolves column refs; detects ambiguous/unresolvable names
- [ ] Diagnostics for syntax (tree-sitter) and semantic errors in real time
- [ ] Hover shows column types and table definitions
- [ ] All intelligence features work offline after initial schema load; no I/O on request path

### Phase 4 — Extensibility & Polish
- [ ] Plugin API stable: versioned, documented, backward-compatible within major versions
- [ ] Core features migrated to plugin API as reference implementations
- [ ] Theme system: light and dark themes, configurable accent colors
- [ ] Query history persisted and browsable
- [ ] Layout state persists across restarts
- [ ] Third-party-style plugin adds a panel and completion provider via public API only
- [ ] Platform-native bundles: .deb, .rpm, AppImage, .dmg, MSI

## Decisions log

> Fine-grained session decisions. MAJOR decisions (architecture, business
> rules, deviations, user directives) live in **docs/DECISIONS.md** — new
> rows for those link `→ D<n>` instead of restating the rationale.

> Older rows (Phase 0 – early Phase 1, 2026-07-13 … 2026-09-03) are archived in [archive/PROGRESS-2026.md](archive/PROGRESS-2026.md).

| Date | Decision | Why |
|---|---|---|
| 2026-09-09 | `.tcat` body codec switched from `bincode` to `postcard` (`default-features = false, features = ["use-std"]`, avoiding postcard's default `heapless`/`atomic-polyfill` chain), `CATALOG_FORMAT_VERSION` bumped 1 → 2, `deny.toml`'s RUSTSEC-2025-0141 ignore removed | → D28 |
| 2026-09-08 | `SchemaObjectId` derived via UUIDv5 over connection id + `(kind, native_id)`, replacing the random id `SchemaService` used to mint | → D25 |
| 2026-09-08 | Incremental refresh diffs the driver's `(oid, xmin)` sweep against cached fingerprints, re-introspecting only touched schemas, with a full-refresh fallback past four thresholds | → D26 |
| 2026-09-08 | `refresh_incremental` returns `(Arc<SchemaSnapshot>, RefreshPath)`; fixed a bug the first live test's full-refresh fallback had been hiding — a dropped-only delta (no `changed` keys) always fell back to `FullRefresh(UnresolvedSchema)` instead of splicing, because schema resolution only walked `delta.changed` and treated an empty result as failure regardless of cause | Review: the live test exercised the documented fallback, not the splice; a rewritten test that changes an existing column instead of adding one caught the real bug |
| 2026-09-08 | `.tcat` catalog cache format (versioned header + bincode body over a private mirror type); D27's Decision sentence corrected in place to name the private `CatalogSnapshot` mirror instead of `SchemaSnapshot` (the only edit DECISIONS.md allows to a settled entry) | → D27 |
| 2026-09-08 | Final whole-branch review of stage 2 fixed a changed-key counterpart to D26's dropped-key Table/View ambiguity (a renamed view's fingerprint key looks like `(Table, oid)` too, so it never matched the cached `View` id and was never re-introspected); factored the candidate-id union into one `candidate_ids` helper shared by both the changed and dropped paths, and made ANY unresolved changed key force a full refresh instead of only "all unresolved" | Review: a partial splice that leaves one changed object stale is worse than a slower full refresh, and the bug was silent — the stale object's new fingerprint gets cached, so no later sweep reports it changed again |
| 2026-09-08 | Final whole-branch review of stage 1 fixed 5 findings in one pass rather than as separate PRs: `ObjectKind`/sweep gained `Index`/`Function` segments, the table query's relkind list aligned with the column query's (`'f'` added), `native_id`'s per-kind-only uniqueness documented, and two stale docs claims corrected; `renumber_second_clause` replaced with a general, digit-scanning `renumber_union_segments` | Stage 1 hadn't shipped yet (no PR merged), so fixing forward on the same branch was cheaper and safer than a follow-up PR against code nothing depends on yet |
| 2026-09-07 | Phase 3 is implemented per docs/superpowers/specs/2026-09-07-phase3-sql-intelligence-design.md, as six sequenced PRs; stage 1 (this branch) is PR 1 of 6, laying `native_id`/`SearchPath`/`schema_fingerprints`/`keywords` at the driver level only | Design decisions 2 and 3 (OID identity, xmin fingerprint sweep) get DECISIONS.md entries (D25/D26) when `SchemaService` actually consumes them in stage 2, per the spec's own rule — not before |
| 2026-09-07 | Workspace `[keybindings]` layer applied at startup from `TEMPR_WORKSPACE`/cwd `workspace.toml`; sync `parse_manifest`/`load_manifest_from` beside the async `Storage` trait | → D24 |
| 2026-09-03 | New utility deps `unicode-segmentation`, `percent-encoding` (both MIT/Apache) | → D18 |
| 2026-09-03 | Scroll performance is measured in-app: `DebugScrollBenchmark` command (ctrl-shift-b) drives `UniformListScrollHandle::scroll_to_item_strict` once per `on_next_frame`, reports fps/p95/max/dropped (> 20 ms); `TEMPR_BENCH_SCROLL=1` + `TEMPR_STARTUP_SQL` make it headless and exit; bench mode disables gpui's inactive-window 30 Hz cap | No screenshot tooling here; a frame-time probe is the only way to verify the 60 fps AC, and it must be a keyboard `Command` per the no-mouse-only rule. Caveat learned: the display must be on — a blanked compositor delivers frames at ~1 Hz |
| 2026-09-03 | Dev knobs are read from env in the binary and passed as `tempr_ui::DevOptions` — the UI crate never touches the environment | Keeps `tempr_ui` config-driven and testable |
| 2026-09-03 | `QueryService::execute_streaming_with_id(run_id, …)` lets the view allocate the `QueryRunId` up front; `cancel` of a not-yet-registered run is armed (`pending_cancels`) and applied when it starts; a cancel landing after registration but before the driver handle is captured aborts before `execute`; cancelling a completed run is a no-op with no event | Review: escape right after Enter used to be silently ignored and an unknown-run cancel published a spurious `QueryFinished{Cancelled}` |
| 2026-09-03 | `MainWindow.current_run: Option<QueryRunId>` is the single owner of "a query is running"; only the outcome task clears it; `QueryFinished` bus events no longer touch view state | Review: the bus `Cancelled` arm raced the outcome task and allowed a second concurrent run |
| 2026-09-03 | `ConnectionService` pools via `deadpool` core over the driver trait; separate 1-slot metadata pool for `SchemaService`; `connect` warms one connection; `Service::stop` drains pools, `QueryService::stop` cancels runs; `stop_all` runs from `on_app_quit` | → D19 |
| 2026-09-03 | `DriverConnection::is_closed` added to the driver trait (sync, no I/O) so the pool evicts dead idle connections on recycle | Review: a dropped socket was re-pooled forever while the UI stayed "Connected" |
| 2026-09-03 | PostgreSQL TLS via rustls + platform roots; `TlsMode` mirrors libpq `sslmode`, default `prefer`; `verify-ca` ≡ `verify-full` | → D20 |
| 2026-09-03 | TLS integration tests run against a second container: `docker run -d --name tempr-pg-tls -e POSTGRES_PASSWORD=tempr -e POSTGRES_USER=tempr -e POSTGRES_DB=tempr -p 55433:5432 -v <dir>/server.crt:/certs/server.crt:ro -v <dir>/server.key:/certs/server.key.src:ro postgres:16-alpine sh -c 'cp /certs/server.key.src /tmp/server.key && chown postgres:postgres /tmp/server.key && chmod 600 /tmp/server.key && exec docker-entrypoint.sh postgres -c ssl=on -c ssl_cert_file=/certs/server.crt -c ssl_key_file=/tmp/server.key'` with a self-signed cert from `openssl req -x509 -newkey rsa:2048 -nodes -days 30 -subj /CN=localhost -addext subjectAltName=DNS:localhost,IP:127.0.0.1`; `DATABASE_URL_TLS=postgres://tempr:tempr@localhost:55433/tempr` | Reproducible local TLS target without touching the plain container |
| 2026-09-03 | `tempr_editor::Buffer`: byte-offset API over `ropey`, `edit` returns `Result`, buffer is a pure model (owner publishes `BufferChanged`), latency criterion checked by an ignored release test | → D21 |
| 2026-09-05 | Commands are GPUI action types in one typed UI catalog; `CommandService` holds metadata + layered keybindings (command → keystrokes, `[]` unbinds); palette emits, `MainWindow` dispatches, service records `CommandExecuted`; in-house fuzzy matcher | → D23 |
| 2026-09-05 | `CommandId(String)` added to the domain (GPUI action name, human-writable in TOML); `AppEvent::CommandExecuted` added | Canonical names for the command system (03/05/06 updated) |
| 2026-09-05 | Overlays add `Modal` to their key context; window-level bindings use `MainWindow && !Modal` (not `!Palette`) and the palette restores the previously focused element on close | One predicate for every future overlay; commands confirmed in the palette dispatch against the element that was focused before it opened |
| 2026-09-05 | A malformed `~/.config/tempr/settings.toml` is non-fatal: defaults are used, the error is logged and shown once in the status bar; a keystroke override that cannot fire falls back to the command's defaults | Bad config must never lock the user out of a command (no mouse-only features) or stop the app from opening |
| 2026-09-05 | `input::*` commands are hidden from palette search; `dispatch` also consults `App::is_action_available` so globally handled actions (`Quit`) run from the palette | Searchable = reachable from the editor/main window; the palette's own `Input` is not a target |
| 2026-09-03 | Syntax tree re-parses lazily on read, not inside `edit`; statement detection is derived from the tree (no hand-written scanner); highlights from the grammar's bundled query | → D22 |
| 2026-09-03 | GPUI code lives in new `tempr_ui` crate (per 14-project-layout.md); binary depends on it; all `gpui`/`gpui_platform`/`gpui_tokio` calls go through `tempr_ui::gpui_compat` | Isolates upstream churn to one module (11-gpui.md shim rule) |
| 2026-09-11 | Every scroll container nested inside (or wrapping) another one sets `restrict_scroll_to_axis` — `ResultGrid`'s `overflow_x_scroll` column container and its `uniform_list` rows both do (11-gpui.md hard rule 6) | → D29 |

## Session log

| Date | Phase | What was done | Follow-ups |
|---|---|---|---|
| 2026-09-11 | Phase 3 (fix) | Branch `fix/todo-scroll-direction`: reproduced the mouse-wheel bug from TODO without the user's Wayland session — no `xdotool`/`ydotool` here and XTest under Xwayland never reaches the window (`query_pointer` child stays 0), so the app ran on `Xvfb :99` with lavapipe (`VK_ICD_FILENAMES=/usr/share/vulkan/icd.d/lvp_icd.json`, `WAYLAND_DISPLAY` unset, `TEMPR_STARTUP_SQL` loading 120 lines + a 3000-row query) and a 60-line x11rb XTest injector (scratchpad `xwheel`: warp pointer, fake buttons 4–7, optional shift) drove the wheel while temporary `tracing::info!` lines logged the root `ScrollWheelEvent`, both `uniform_list` offsets/`max_offset`/`last_item_size` and the outer container's `ScrollHandle`. Evidence: wheel arrives as `ScrollDelta::Lines{y: ±3}` (shift → `x: ±3`), the editor scrolls fine, but over the grid the outer `overflow_x_scroll` div moved by the *vertical* delta (`outer.x == inner.y` after every tick) and shift-wheel moved the rows vertically — gpui's cross-axis fallback in `Interactivity::paint_scroll_listener` (`delta_x = delta.y` when only `overflow.x` scrolls and `restrict_scroll_to_axis` is unset, and vice versa). Fix: `.restrict_scroll_to_axis()` on the container, the same flag via `interactivity().base_style` on the rows list (not exposed on `UniformList`), and `.min_w_full()` on the column so a 2-column result still scrolls when the pointer is right of the rows (before, the list hitbox was 360 px wide and the wheel there did nothing). Re-verified on Xvfb: narrow and wide results, wheel/shift-wheel/tilt, pointer over and beside the rows. Instrumentation removed; fmt/clippy/19 `tempr_ui` tests green. Also parked the Zed agent-skill adoption steps (gpui-test, gpui-bench, dylint trial) in TODO. **Open:** the user reported the bug on GNOME Wayland and also for the editor — the editor scrolled correctly under X11 here, so the Wayland session still needs a hands-on check after this lands (wheel over editor and grid, with and without shift) | user confirms the fix on Wayland; if the editor still fails there, instrument `gpui_linux` Wayland `AxisValue120` handling next |
| 2026-09-08 | Phase 3 | Branch `feat/ph3-catalog-persistence` (stage 2 of 6): `SchemaObjectId::derived` (UUIDv5 over connection + `(kind, native_id)`, golden-value-pinned, → D25) replaces `SchemaService`'s random id; `SchemaSnapshot` gained `keywords`/`fingerprints` (`#[serde(default)]`, backward-compatible with pre-stage-2 caches); new `.tcat` codec in `tempr_workspace::catalog` — 16-byte header (magic/version/flags/FNV-1a hash) + `bincode` body over a private externally-tagged `CatalogSnapshot`/`CatalogObject` mirror (bincode can't decode `SchemaObject`'s internally-tagged JSON shape directly), any header/decode mismatch discards to `Ok(None)` rather than erroring (→ D27, `bincode` added under the D18 small-pure-Rust-dep rule, `cargo deny` ignore for RUSTSEC-2025-0141 added); `Storage::catalog_cache`/`CatalogCacheFile` (non-async handle, async `load`/`save`, each `save` call gets a uniquely-named temp file so concurrent saves for one connection can't race the same inode — landed as its own fix after the first version shared one temp path); `SchemaService::with_cache` persists post-refresh (best-effort, `snapshot_content_hash` skips a rewrite when only bookkeeping changed) and `load_cached` reads without a database connection; `refresh` moved from `SchemaScope::All` to `SchemaScope::SearchPath`, now derives ids and passes through `estimated_rows`/view `definition` instead of hardcoding them, and drops children whose parent fell out of scope; `refresh_incremental` (→ D26) diffs the fingerprint sweep against cached fingerprints, re-introspects only touched schemas via `splice_objects`, and falls back to a full `refresh` past four thresholds (no cached fingerprints, no driver support, an object the cache never saw, >40% touched) — a dropped relation's id is removed under both `Table` and `View` derivations since the sweep can't say which it was. Task 7 (this pass): 4 new live-PostgreSQL integration tests (`pg_object_ids_survive_a_second_refresh`, `pg_incremental_refresh_splices_a_changed_column_and_a_dropped_table`, `pg_incremental_refresh_falls_back_to_full_refresh_on_an_added_column`, `pg_catalog_is_readable_without_the_server` — the last exercises the phase's offline criterion with a `ConnectionService` that has no route to the database); D25/D26 recorded, D27's Decision sentence corrected (private mirror, not `SchemaSnapshot`); docs/07-storage.md OD#1 resolved with the real `CatalogCacheFile` surface and `.tcat` layout; docs/05-services.md `SchemaService` surface brought current (`new`/`with_cache`/`refresh`/`refresh_incremental`/`load_cached`/`snapshot`/`version`); docs/TODO.md closed the `estimated_rows`/`definition` and stale-`SearchPath` rows, removed the now-done "Phase 3: Catalog cache" tracker line, added cache eviction, per-connection catalog scope, the `save_manifest` temp-file race, native-id-on-`SchemaObject`, and the bincode-advisory-revisit rows. Full verification: 188 unit tests, 27 PG integration tests (plain + TLS, 3 new), fmt/clippy/deny clean; the only failure is the known debug-build timing probe (`perf_large_batch_edit_is_linear`). **Phase 3 stage 2 complete — both catalog-cache checklist boxes checked**. Review pass on this task found the live incremental test never exercised the splice (`ADD COLUMN` always takes the documented fallback) and asked for the path to be made observable: `refresh_incremental` now returns `(Arc<SchemaSnapshot>, RefreshPath)` (`Unchanged` / `Incremental{schemas, touched}` / `FullRefresh(FullRefreshReason)`); the rewritten tests (`ALTER COLUMN ... TYPE` for a changed-only delta, `DROP TABLE` for a dropped-only delta, `ADD COLUMN` kept as the documented fallback) caught a real bug the fallback had been masking — a dropped-only delta always fell back instead of splicing, because schema resolution looked only at `delta.changed` — now fixed; D26's Consequences gained the dropped-Table-or-View sentence Finding 2 asked for; all `public`-schema-mutating live tests plus the two comparison tests share `SCHEMA_REFRESH_LIVE_TEST_LOCK` (`SchemaScope::SearchPath` always includes `public`, so a sweep-comparing test can't tolerate concurrent DDL from another live test in this same binary) — stable across 10 repeated `--ignored` runs afterward, where it had failed on roughly half of runs before. Full re-verification: 188 unit tests, 28 PG integration tests (plain + TLS, 4 this stage), fmt/clippy/deny clean. Final whole-branch review fix pass (this session, 5 findings): the changed-key path had the same `Table`-folds-`View` ambiguity D26 already documents for dropped keys — a renamed view's fingerprint key looks like `(Table, oid)`, never matched the cached `View` id, and was silently never re-introspected again — fixed by factoring both paths' candidate-id union into one `candidate_ids` helper, and by falling back to a full refresh (`FullRefreshReason::UnresolvedSchema`) when ANY changed key is unresolved rather than only when all are; `object_schema`/`splice_objects` now build an id→object `HashMap` once per splice instead of linear-scanning the object list per lookup (O(n²) → O(n) at the spec's 10,000-object target); the fingerprint sweep's index segment now matches `relkind IN ('i', 'I')` so a partitioned index can be swept and reported dropped (`snapshot_schema`'s other relkind lists checked against the sweep's — no other mismatch found); `load_cached` now discards a cache whose decoded `connection_id` doesn't match the connection it was loaded for (logged, not silently trusted); this doc's next-action line corrected (stage 3 is the `tempr_intel` in-memory catalog crate, not completion — that's stage 5, spec §9). New live test `pg_incremental_refresh_resolves_a_renamed_view_on_a_non_public_schema` (padded with filler tables so the rename stays under the 40%-touched threshold and actually exercises the splice, not the fallback — a bug hiding behind a threshold-triggered fallback is exactly the class of mistake D26's own review already made once). docs/TODO.md gained 5 rows: stale cached `keywords` never refreshed by a splice, the write-skip dedup's sensitivity to catalog-query ordering, `save_cached`'s full-decode-just-to-hash cost, `SchemaService`'s missing per-connection concurrency guard (no caller today, but stage 6 wires refresh commands to keybindings), and `refresh_incremental` returning a path label instead of the delta stage 3's in-memory catalog will actually want. Full re-verification: 188 unit tests, 29 PG integration tests (plain + TLS, 1 new), fmt/clippy/deny clean, stable across two `--ignored` runs. Dependency follow-up (this session, same branch — PR not yet merged): `cargo deny` flagging `bincode` unmaintained (RUSTSEC-2025-0141) prompted swapping the `.tcat` body codec to `postcard` (`postcard::to_allocvec`/`from_bytes`, `default-features = false, features = ["use-std"]` to keep its default `heapless`/`atomic-polyfill` chain — itself unmaintained, RUSTSEC-2023-0089 — out of the build); `CATALOG_FORMAT_VERSION` bumped 1 → 2 so a pre-swap `.tcat` is discarded and re-introspected rather than misread; the private externally-tagged `CatalogObject`/`CatalogSnapshot` mirror is unchanged — postcard has the same `deserialize_any` limitation bincode did, so it's still required; `deny.toml`'s RUSTSEC-2025-0141 ignore removed outright (→ D28, supersedes D27's format choice); docs/07-storage.md's `.tcat` section and OD#1 updated, docs/TODO.md's bincode-advisory-revisit row removed (resolved, not deferred). Full re-verification: 188 unit tests, 29 PG integration tests (plain + TLS, no new tests), fmt/clippy/deny clean (deny confirmed clean both with the swap and without any lingering advisory ignore) | Phase 3 stage 3: the `tempr_intel` crate — in-memory `CatalogCache`, lookups, load probe (spec §9) |
| 2026-09-08 | Phase 3 | Final whole-branch review fix pass on `feat/ph3-driver-identity` closed 5 findings: `ObjectKind` gained `Index`/`Function`; `schema_fingerprints` now unions 4 segments (relations, columns, indexes scoped through their parent table like `snapshot_schema`'s index query, functions via `function_scope_clause`) instead of missing `CREATE/DROP INDEX` and `CREATE OR REPLACE FUNCTION` entirely; `snapshot_schema`'s table query relkind list gained `'f'` so it matches the column query's and foreign tables get a `Table` entry instead of orphaned columns; `native_id`'s "unique only within `kind`, key on `(kind, native_id)`" contract documented on `SchemaSnapshotEntry` and `SchemaFingerprint`; docs/09-database-engine.md documents the `attnum`-ordinal-gap-after-`DROP COLUMN` and no-longer-privilege-filtered-columns changes; docs/TODO.md's stale "`SearchPath` is the only refresh scope" claim corrected to `All`, and its now-resolved `renumber_second_clause` digit-boundary item removed. Also replaced `renumber_second_clause` (single `UNION ALL` split, substring `$N` replace) with `renumber_union_segments`/`shift_placeholders` — per-segment bind counts (the function segment binds fewer values than the relation/column/index segments for `SchemaScope::Table`) and digit-run scanning so `$1` can never match inside `$10`. 3 new integration tests (index-change, function-change, `postgres_fdw` foreign-table) + 4 new unit tests for the renumbering helper (net +4/+3 after removing the 2 superseded tests). Full verification: 161 unit tests, 24 PG integration tests (plain + TLS), fmt/clippy clean; the only failure is the known debug-build timing probe (`perf_large_batch_edit_is_linear`) | Stage 2: catalog persistence — derive `SchemaObjectId` from `(kind, native_id)`, persist snapshots, incremental refresh via `schema_fingerprints` |
| 2026-09-07 | Phase 3 | Branch `feat/ph3-driver-identity` (stage 1 of the six-PR Phase 3 plan in docs/superpowers/specs/2026-09-07-phase3-sql-intelligence-design.md): `tempr_db`/`tempr_db_postgres` introspection moved from `pg_tables`/`information_schema` onto `pg_catalog` so every `SchemaSnapshotEntry` carries a stable `native_id` (`pg_class.oid`/`pg_proc.oid`/`(attrelid << 16) \| attnum`); added `SchemaScope::SearchPath` (`current_schemas(false)` + `public`), for stage 2's `SchemaService` to adopt as the refresh scope — `SchemaService::refresh` still passes `SchemaScope::All` this stage, unchanged — a `Function` entry (OUT-parameter-safe argument naming via `proargmodes` filtering), `DriverConnection::schema_fingerprints` (per-object `xmin`, `Unsupported` default) and `DriverConnection::keywords` (`pg_get_keywords()`, empty default); `data_type` now comes from `format_type` instead of `information_schema` (`varchar(50)` reports with its length); 6 new integration tests; driver-trait contract documented in 09-database-engine.md. Full verification: 157 unit tests, 21 PG integration tests (plain + TLS), fmt/clippy clean; the only failure is the known debug-build timing probe (`perf_large_batch_edit_is_linear`). Deliberately deferred to stage 2: `SchemaService` still mints random `SchemaObjectId`s, no caching, no identity consumption — see TODO | Stage 2: catalog persistence — derive `SchemaObjectId` from `native_id`, persist snapshots, incremental refresh via `schema_fingerprints` |
| 2026-09-07 | Phase 2 | Interactive check of the Phase 2 palette + editor boxes against Docker PG (user-driven, Wayland): box 4 (ctrl-shift-p, fuzzy filter, enter dispatch, esc + focus restore, arrow scrolling) and box 7 (`select 1 as a; select 2 as b;` — per-statement ctrl-enter, ctrl-z undo) both pass, boxes checked. Branch `fix/palette-row-layout`: palette rows are `w_full` + `gap_4` so `justify_between` right-aligns the keystrokes (they used to butt against the category: `Editorctrl-c`); `UniformListScrollHandle` attached to the palette list with `scroll_to_item(.., Nearest)` on arrow moves and a reset to row 0 on a new query (arrows previously moved the selection off-screen — the counter read "25 of 37" while the list showed rows 1–12); status line pluralization via `row_word` (+1 test). 153 tests | Phase 2 exit sweep; workspace keybinding layer on workspace open | Then branch `chore/ph2-exit-sweep`: Phase 2 exit sweep — box 5 was the one unmet criterion (nothing ever read a `workspace.toml`), closed by resolving the manifest path in the binary (`TEMPR_WORKSPACE`, else cwd) and passing its `[keybindings]` as the top layer, with sync `parse_manifest`/`load_manifest_from` added to `tempr_workspace::manifest` (+2 tests, → D24); verified with `TEMPR_LIST_COMMANDS=1` (rebind + unbind + corrupt-file fallback). Swept docs: Phase 2 checklist and PRODUCT section 3 marked ✅ (including the keyboard-only AC, which had lagged), TODO lost four completed Phase 2 rows and two live-rebind rows merged into one, CLAUDE.md status + phase pointer, 04-workspace settings layering. Full verification: 155 unit, 15 PG integration (TLS container re-created), release perf probes, `cargo deny`. **Phase 2 complete**
| 2026-09-05 | Phase 2 | PR #10 (syntax tree) review fixes + PR opened; SSH agent had no key → pushed over HTTPS with `gh`. Branch `feat/ph2-command-palette` (stacked on #10): `CommandId` + `CommandExecuted`, `CommandService` (+ fuzzy matcher), `UserSettings` loader + manifest `[keybindings]`, typed command catalog `tempr_ui::commands` (21 commands, audit test), `Palette` component, `MainWindow` toggle/dispatch, `TEMPR_LIST_COMMANDS=1`; boxes 5 + 8 checked, box 4 implemented pending interactive check (→ D23); 136 unit tests. Branch `feat/ph2-editing-ops`: `Selection`, motions, cursor-based edit ops with selection-aware undo/redo — box 6 checked; 147 unit tests. Branch `feat/ph2-editor-view`: `EditorView` (multi-line editor replacing the SQL `Input`; 33 editor commands), theme highlight colors, `Buffer` utf16/line helpers; palette review (8 findings) fixed; box 5 downgraded to partial (workspace layer not yet applied), box 7 implemented pending interactive check ; consolidated review of editing-ops + editor-view (three passes, ~20 findings) → `HighlightKind` enum replaces capture strings, EOF-cursor `statement_at`, column-0 line-block exclusion, per-block `move_lines`, backspace/delete undo snapshots, per-frame `line_layouts` + IME host fallback line, nested-capture-safe runs, newline selection paint, `!Modal` predicate + palette focus restore, `Quit` dispatchable from the palette, fireable-keystroke validation with default fallback, `input::*` hidden from search, settings parse error shown in the status bar; 152 unit tests | User: merge #10, then the stacked PR; ctrl-shift-p and ctrl-enter checks; Phase 2 exit sweep; workspace keybinding layer on workspace open |
| 2026-07-13 | Phase 0 | Architecture suite (16 docs + 9 ADRs + RFC) written; Cargo workspace + 5 crates scaffolded; domain types (15 tests), event bus (6), service registry (5), workspace manifest (4), storage (6) implemented; CI workflow + cargo-deny configured; MIT license set (→ D12); all 8 exit criteria verified; PR review workflow (D13) adopted: pre-push hook + setup script + CLAUDE.md hard rule; OD#5 resolved: upstream git pin, no fork (→ D14); lorekeeper check: zero drift; D13 judgment exception eliminated — no direct commits to main ever (→ D15) | Begin Phase 1; run bash scripts/setup.sh in each new worktree |
| 2026-07-14 | Phase 1 | Created `feat/phase1-db-layer` branch; implemented database layer: `tempr_db` driver traits crate, `tempr_db_postgres` PostgreSQL driver (tokio-postgres, batched streaming, PG type decode), extended `Value` enum (8 new variants + `ValueType`/`ColumnSpec`/`Batch`), `ConnectionService` (pool + `with_connection_fn` exclusive access), `QueryService` (execute → stream → event lifecycle), `SchemaService` (PG introspection + snapshot), binary wiring; fixed compilation: DriverConnection `Send + Sync`, PostgresStream pinning, service API redesign; 37 tests pass, clippy clean, fmt clean | Wire deadpool-postgres; add PG integration tests; GPUI application shell |
| 2026-07-14 | Phase 1 | Fixed 5 failing tests (bool decode: `t`/`f` format, timestamp timezone offset `+00` handling), fixed ConnectionService missing `Failed` state on no-driver path, added `ServiceError::{QueryFailed, ConnectionNotFound, NotConnected}`, added 7 integration tests (PG connect/select/insert/streaming/auth/schema/events), parameterized schema snapshot queries (SQL injection fix), pinned PostgresStream (`Pin<Box<RowStream>>`); **65 unit tests pass** — clippy clean, fmt clean, cargo deny clean | Run integration tests with DATABASE_URL; begin GPUI shell |
| 2026-07-14 | Phase 1 | Fixed Docker PostgreSQL compatibility: switched from `query_raw` to `client.query()` (lifetime issue with RowStream borrowing client), added `password` field to `Connection` struct, switched `sslmode=require` to `sslmode=disable`, added `PostgresStream::from_rows` for collected results; **all 7 integration tests pass** against Docker PostgreSQL | Commit and merge; begin GPUI shell |
| 2026-08-17 | Phase 1 | Branch `docs/gpui-verified-api`: folded the verified GPUI API survey (local `zed-industries/zed` clone, gpui `0.2.2`) into docs/11-gpui.md — corrected `Render` trait + 2-arg `render(&mut Window, &mut Context<Self>)` signature, `Entity<T>`/`cx.new` state model, `gpui_platform::application()` entry point, dependency wiring (two crates, one rev), what GPUI actually ships (no text input, no 2D grid, no usable theme), `uniform_list`/`list`+`ListState` virtualization APIs, GPUI executor vs tokio (`gpui_tokio` bridge), Zed crate licensing (GPL `ui`/`theme`/`markdown`/`editor` out of bounds); resolved 2 open questions; **D16** recorded | Verify `gpui_tokio` license; add `rust-toolchain.toml` ≥ 1.95.0; PR |
| 2026-09-03 | Phase 1 | Branch `feat/ph1-gpui-deps`: `rust-toolchain.toml` (1.97.1); `gpui`/`gpui_platform`(features wayland,x11)/`gpui_tokio` pinned at Zed `main` `ed8d600`; `cargo deny` caught GPL `zlog`/`ztracing`/`ztracing_macro` at tag v1.9.0 → re-pinned past upstream relicense `ac5af8b9` (→ D17); deny allowlist +Zlib/CC0-1.0/bzip2-1.0.6/NCSA, 3 unmaintained advisories ignored with reasons, yanked `chacha20` bumped; `.cargo/config.toml` git-CLI fetch; CI installs Linux gpui libs + reads toolchain file; new `tempr_ui` crate (`gpui_compat`: `run_app`/`open_main_window`/`spawn_tokio`; `MainWindow` placeholder view); binary rewritten: sync `main`, services built pre-GPUI, started via `Tokio::spawn`; fmt/clippy/test/deny green; window verified 1200×800; **PR #4 merged**. Then branch `feat/ph1-input-grid`: `RowSink` + `QueryService::execute_streaming` (+2 tests), `tempr_ui::components::{Input, ResultGrid}`, `events::bridge` (`UiEvent`), `MainWindow` wiring (connect on start, Enter/ctrl-enter runs, rows stream per batch, status bar), `DATABASE_URL` connection in binary, `value_format` (+4 tests); 76 unit + 10 PG integration tests (new `pg_execute_streaming_100k_rows_in_batches`) against Docker PG 16 on :55432; dev libs installed by user (no `LIBRARY_PATH` needed); **PR #5 merged** (user screenshot verified rendering). Then branch `feat/ph1-grid-perf-cancel`: `ScrollBench` (+3 tests), `DebugScrollBenchmark`/`CancelQuery` actions, `DevOptions` (`TEMPR_STARTUP_SQL`, `TEMPR_BENCH_SCROLL`), `ResultGrid` scroll handle; code review (8 findings) → `track_scroll` had never been attached (the first bench measured a static viewport), eager run id + pending cancels, `spawn_in`/`update_in` instead of a render hook, headless abort on every terminal path, strict scroll; headless bench numbers discarded (display was off → 1 s frames); user re-ran with display on: **57.2 fps / avg 17.5 / p95 23.1 ms / 61 dropped of 603** — Phase 1 grid box checked with the 10%-slow-frames caveat; 82 unit tests; **PR #6 merged**. Then branch `feat/ph1-service-lifecycle-pool`: `deadpool`-backed `ConnectionService` (user pool + metadata slot, `PooledConnection`, `PoolConfig`, tests for pool growth/reuse/metadata isolation/stop), `Service` impls for the three core services + registry registration, `SchemaService` on the metadata slot, `deadpool-postgres` dropped (→ D19); review (8 findings): `is_closed` on the driver trait + recycle eviction, `stop_all` wired to `on_app_quit`, pool + PG connect timeouts, single `ConnEntry` map with Connecting guard and race-safe insert, bounded concurrent `cancel_all`, `?`-style execute closure, doc names; 90 unit tests; **PR #7 merged**. Then branch `feat/ph1-pg-tls`: `TlsMode` (+2 domain tests), rustls connector module in `tempr_db_postgres` (+2 tests), `?sslmode=` in `DATABASE_URL`, manifest `tls` field, 5 TLS integration tests vs a self-signed `ssl=on` container (→ D20); review (8 findings): SqlState-based error classification with cause chain, libpq `prefer` plaintext fallback, connectors built once and shared, per-run cancel timeout, `TlsChoice` removed, `TlsMode` API trimmed, sharper TLS tests; CLAUDE.md phase pointer flipped, 26 decisions-log rows archived; 94 unit + 15 integration — **Phase 1 checklist complete**; **PR #8 merged**. Then branch `feat/ph2-rope-buffer`: new `tempr_editor` crate, `Buffer` on `ropey` (byte-offset API, atomic validated batch edits, undo/redo transactions with final-position bookkeeping, point↔offset with CRLF/multibyte), 9 tests + 10 MB release probe 1.73 µs avg (→ D21); review (8 findings) → LIFO undo, tie-breaks, no-op edits, LF/CRLF-only lines; 109 unit tests; **PR #9 merged**. Then branch `feat/ph2-syntax-tree`: `SyntaxTree` on `tree-sitter-sequel` (→ D22), lazy incremental reparse from recorded `InputEdit`s, statement ranges / `statement_at`, highlights; probes: edit 5.9 µs, reparse 1.6 ms realistic / 150 ms pathological; review (8 findings) → `StatementKind` (Error/Block/Transaction flagged), allocation-free `statement_at` with exclusive-end tie-break, Tempr-owned `highlights.scm` (`[0-9]` classes, no `@spell`, last pattern wins), `Buffer::new` no longer parses, no-op edits skip tree bookkeeping, `file_id()` restored; 120 unit tests | command palette + keybindings; editing ops; execute-statement-under-cursor; workspace open + connection picker | TLS connector for PG (last Phase 1 box); workspace open + connection picker; `DriverConnection::ping` health checks; profile the 10% > 20 ms frames |
| 2026-07-14 | Phase 1 | Code review fix pass (10 findings): fixed typed column decoding (finding #1), params passthrough (#2), RETURNING rows (#3), batch-size chunking (#4), schema error propagation (#5), schema scope for columns/indexes (#6), index columns via pg_index (#7), cancel handle capture (#8), configurable sslmode (#9), conninfo escaping via Config builder (#10); reverted #4 from `query_raw` to `query()`+chunked batch due to live-DB `Closed` error with `query_raw` through QueryService (true lazy streaming deferred to TODO); cleaned up debug pollution from root-cause investigation | Lazy wire streaming as follow-up; commit and PR |
