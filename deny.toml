[advisories]
version = 2
# Unmaintained-crate notices pulled in transitively by gpui (font shaping / SVG
# stack: cosmic-text → fontdb → ttf-parser; resvg → usvg → rustybuzz; xkbcommon
# → paste). Not fixable from Tempr; revisit on every gpui rev bump.
ignore = [
    { id = "RUSTSEC-2024-0436", reason = "paste unmaintained — transitive via gpui's linux input stack" },
    { id = "RUSTSEC-2026-0192", reason = "ttf-parser unmaintained — transitive via gpui (cosmic-text/fontdb, usvg)" },
    { id = "RUSTSEC-2026-0206", reason = "rustybuzz unmaintained — transitive via gpui (resvg/usvg)" },
    # bincode, a DIRECT dependency chosen in D27 for the .tcat catalog codec:
    # the bincode team ceased development permanently after a doxxing/
    # harassment incident (not a code defect) and call 2.0.1 stable; no fix
    # is available. Accepted for now under D27's own terms (small, pure-Rust,
    # already-serde-shaped) — revisit at the spec's §8 load probe alongside
    # the rkyv/SQLite alternatives D27 already weighed.
    { id = "RUSTSEC-2025-0141", reason = "bincode unmaintained (team ceased development after a harassment incident); accepted per D27, revisit at the load probe" },
]

[licenses]
version = 2
allow = [
    "MIT",
    "Apache-2.0",
    "Apache-2.0 WITH LLVM-exception",
    "MPL-2.0",
    "ISC",
    "BSD-2-Clause",
    "BSD-3-Clause",
    "Unicode-3.0",
    # Permissive licenses pulled in transitively by gpui's dependency graph
    "Zlib",
    "CC0-1.0",
    "bzip2-1.0.6",
    "NCSA",
]

[bans]
multiple-versions = "warn"

[sources]
unknown-registry = "deny"
unknown-git = "deny"
allow-git = [
    # gpui + gpui_platform + gpui_tokio, pinned to one rev (D14/D16)
    "https://github.com/zed-industries/zed",
]

[sources.allow-org]
# gpui pulls transitive forks from the zed-industries org (e.g. zed-font-kit on macOS)
github = ["zed-industries"]
